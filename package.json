{
	"name": "mati-news",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "nodemon App.js ",
		"client": "npm run start --prefix client",
		"dev": "concurrently \"npm run start\" \"npm run client\"",
		"install-all": "npm install && cd client && npm install && npm run build",
		"heroku-postbuild": "cd client && npm install && npm run build"
	},
	"keywords": [],
	"author": "mati masganow",
	"license": "ISC",
	"dependencies": {
		"axios": "^0.25.0",
		"concurrently": "^7.0.0",
		"express": "^4.17.2",
		"mongodb": "^4.3.1",
		"newsapi": "^2.4.1",
		"nodemon": "^2.0.15",
		"socket.io": "^4.4.1"
	}
}
